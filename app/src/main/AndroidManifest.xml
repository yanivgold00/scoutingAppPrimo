<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a4586.primo.primoscoutingapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ScoutingChooseActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".AutonomousActivity" />
        <activity android:name=".TeleopActivity" />
        <activity android:name=".EndGameActivity" />
        <activity android:name=".PitFormActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PitMainActivity" />
        <activity android:name=".Pit2MainActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".SendActivity"></activity>
    </application>

</manifest>